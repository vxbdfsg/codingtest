#include <string>
#include <vector>
#include<cmath>

using namespace std;

long long solution(long long n) {
    long long answer = 0;
    long long int temp = sqrt(n);
    if(temp == sqrt(n)) return (temp+1)*(temp+1);
    else return -1;
    return answer;
}
