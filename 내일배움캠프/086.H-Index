function solution(cit) {
    var answer = 0;
    cit.sort(function (a, b) {return b - a;});
    for(var i = 0 ; i < cit.length ; i++){
        if(cit[i] >= i+1) answer = i+1;
    }
    console.log(cit);
    return answer;
}
