SELECT AI.ANIMAL_ID,
AI.ANIMAL_TYPE,
AI.NAME
FROM ANIMAL_INS AI JOIN ANIMAL_OUTS AO ON AI.ANIMAL_ID = AO.ANIMAL_ID
WHERE substr(AI.SEX_UPON_INTAKE, 1, 6) = 'Intact' 
AND substr(AO.SEX_UPON_OUTCOME, 1, 6) != 'Intact'
ORDER BY AI.ANIMAL_ID
